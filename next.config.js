/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  transpilePackages: ['three'],
  images: {
    domains: ['dhanrajsp.me'],
  },
};
 
module.exports = nextConfig;